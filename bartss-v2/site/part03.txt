,identify:1,ribbons:0,transform:0,orangePanel:0,copy:4,p:{green:[363,390,.20],yellow:[858,468,.20],blue:[913,468,.20],orange:[736,416,.20],purple:[761,316,.20]}},
    {bg:'#c5fd77',hero:0,rail:0,identify:0,ribbons:1,transform:0,orangePanel:0,copy:0,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}},
    {bg:'#7a79f1',hero:0,rail:0,identify:0,ribbons:1,transform:0,orangePanel:0,copy:0,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}},
    {bg:'#7a79f1',hero:0,rail:0,identify:0,ribbons:1,transform:0,orangePanel:0,copy:0,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}},
    {bg:'#070707',hero:0,rail:0,identify:0,ribbons:0,transform:1,orangePanel:0,copy:1,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}},
    {bg:'#070707',hero:0,rail:0,identify:0,ribbons:0,transform:1,orangePanel:0,copy:2,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}},
    {bg:'#070707',hero:0,rail:0,identify:0,ribbons:0,transform:1,orangePanel:0,copy:3,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}},
    {bg:'#070707',hero:0,rail:0,identify:0,ribbons:0,transform:1,orangePanel:0,copy:4,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}},
    {bg:'#070707',hero:0,rail:0,identify:0,ribbons:0,transform:1,orangePanel:215,copy:4,p:{green:[OFFL,0,.1],yellow:[OFFL,0,.1],blue:[OFFL,0,.1],orange:[OFFL,0,.1],purple:[OFFL,0,.1]}}
  ];
  const lerp=(a,b,t)=>a+(b-a)*t, smooth=t=>t*t*(3-2*t);
  function mixHex(a,b,t){
    const pa=[parseInt(a.slice(1,3),16),parseInt(a.slice(3,5),16),parseInt(a.slice(5,7),16)];
    const pb=[parseInt(b.slice(1,3),16),parseInt(b.slice(3,5),16),parseInt(b.slice(5,7),16)];
    const c=pa.map((v,i)=>Math.round(lerp(v,pb[i],t)).toString(16).padStart(2,'0')).join('');
    return '#'+c;
  }
  function setIdentifyCopy(level){
    const icon=(name)=>`<span class="inline mini ${name}"><span class="icon">${cards[name].querySelector('.icon').innerHTML}</span></span>`;
    const pieces=[
      `<span class="identify-line">Your ${icon('purple')} brand.</span>`,
      `<span class="identify-line">That ${icon('yellow')} digital product.</span>`,
      `<span class="identify-line">The ${icon('blue')}${icon('orange')} attention you need.</span>`,
      `<span class="identify-line">And the ${icon('green')} content it takes to earn it.</span>`
    ];
    identifyCopy.innerHTML=pieces.slice(0,level).join('');
  }
  function setTransformCopy(level){
    const ps=[...transformCopy.querySelectorAll('p')];
    ps.forEach((p,i)=>{ p.style.opacity = i<level ? '1' : (i===0 ? '0.36' : '0.18'); });
  }
  function renderBase24(p){
    p=Math.max(0,Math.min(1,p)); const s=p*(frameData.length-1); const i=Math.min(frameData.length-2,Math.floor(s)); const t=smooth(s-i), a=frameData[i], b=frameData[i+1];
    bg.style.background=mixHex(a.bg,b.bg,t);
    hero.style.opacity=lerp(a.hero,b.hero,t).toFixed(3);
    rail.style.opacity=lerp(a.rail,b.rail,t).toFixed(3);
    identify.style.opacity=lerp(a.identify,b.identify,t).toFixed(3);
    ribbons.style.opacity=lerp(a.ribbons,b.ribbons,t).toFixed(3);
    transform.style.opacity=lerp(a.transform,b.transform,t).toFixed(3);
    orangePanel.style.height=lerp(a.orangePanel,b.orangePanel,t)+'px';
    names.forEach(n=>{ const x=lerp(a.p[n][0],b.p[n][0],t), y=lerp(a.p[n][1],b.p[n][1],t), sc=lerp(a.p[n][2],b.p[n][2],t); cards[n].style.left=x+'px'; cards[n].style.top=y+'px'; cards[n].style.transform='scale('+sc+')'; });
    setIdentifyCopy(Math.round(lerp(a.copy,b.copy,t)));
    if (i>=19) setTransformCopy(Math.max(1, Math.round(lerp(Math.max(a.copy-18,1), Math.max(b.copy-18,1), t)))); else setTransformCopy(1);
  }

  function setCharState(frame){
    characterScene.style.opacity='1';
    bg.style.background='#ff6d41'; hero.style.opacity='0'; rail.style.opacity='0'; identify.style.opacity='0'; ribbons.style.opacity='0'; transform.style.opacity='0';
    charEls.forEach(el=>el.style.opacity='0'); characterPurple.style.opacity='0'; slash1.style.opacity='0'; slash2.style.opacity='0';
    characterTitle.style.opacity='0'; characterSub.style.opacity='0'; characterCopy.style.opacity='0';
    const show=(id)=>document.getElementById('char'+id).style.opacity='1';
    if(frame<=27){show('025'); characterTitle.style.opacity='1'; characterSub.style.opacity='1'; if(frame===27){characterTitle.style.opacity='.25'; characterSub.style.opacity='.25';}}
    else if(frame<=30){show('028'); if(frame>=29) characterCopy.style.opacity='1'; if(frame===30){characterTitle.style.opacity='1'; characterTitle.textContent='Meet Bartss'; characterSub.style.opacity='0'; characterCopy.style.opacity='0';}}
    else if(frame===31){show('031');}
    else if(frame<=37){show('032'); characterCopy.style.opacity='1';}
    else if(frame===38){show('038');}
    else {show('039'); characterPurple.style.opacity='1'; slash1.style.opacity='1'; slash2.style.opacity='1';}
  }
  function renderAll(frameFloat){
    const f=Math.max(1,Math.min(39,frameFloat));
    if(f<=24){ characterScene.style.opacity='0'; renderBase24((f-1)/23); return; }
    const fr=Math.round(f); setCharState(fr);
  }
  const params=new URLSearchParams(location.search), frame=params.get('frame');
  if(frame){ const idx=Math.max(1,Math.min(39,parseInt(frame,10)||1)); renderAll(idx); return; }
  function onScroll(){ const rect=story.getBoundingClientRect(), max=rect.height-innerHeight; renderAll(1 + (max>0?(-rect.top/max):0)*38); }
  onScroll(); addEventListener('scroll',onScroll,{passive:true}); addEventListener('resize',onScroll);
})();
</script>
</body></html>