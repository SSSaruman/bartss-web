(()=>{const world=document.getElementById('world');const rail=[...document.querySelectorAll('.capability-rail button')];if(!world)return;rail.forEach(btn=>{btn.addEventListener('pointerenter',()=>world.dataset.focus=btn.dataset.focus);btn.addEventListener('pointerleave',()=>delete world.dataset.focus);});if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;let tx=0,ty=0,cx=0,cy=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5)*10;ty=(e.clientY/innerHeight-.5)*10;},{passive:true});function tick(){cx+=(tx-cx)*.06;cy+=(ty-cy)*.06;world.style.transform=`translate3d(${cx}px,${cy}px,0)`;requestAnimationFrame(tick)}tick();})();